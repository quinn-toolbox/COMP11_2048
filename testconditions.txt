TEST CASES:
- returning the highest score in normal conditions and returning the
  highest score when the list is empty (should return 0)
- inserting to the front, middle, and end of list
- printing top 5 in normal conditions and when fewer than 5
  (should print all)
- print in normal conditions (DONE) and when there are no entries
  (should print nothing)
- print user in normal conditions and when there is no user of that name
  (should print nothing)
- delete user: same as print user (should delete nothing)
- keep top 10 in normal conditions and when fewer than 10 (should keep all)
- clear in normal conditions and when linked list is already empty 
